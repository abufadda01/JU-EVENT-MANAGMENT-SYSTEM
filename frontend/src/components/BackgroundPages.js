const BackgroundPages = (props) => {
  return (
    <section className="blog-background-section">
      <div className="background ">
        <h1>{props.title}</h1>
      </div>
    </section>
  );
};

export default BackgroundPages;
